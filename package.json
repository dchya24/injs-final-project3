{
  "dependencies": {
    "bcrypt": "^5.0.1",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "joi": "^17.6.0",
    "jsonwebtoken": "^8.5.1",
    "pg": "^8.7.3",
    "sequelize": "^6.19.0"
  },
  "devDependencies": {
    "jest": "^28.1.0",
    "node-mocks-http": "^1.11.0",
    "nodemon": "^2.0.16",
    "sequelize-cli": "^6.4.1",
    "sequelize-mock": "^0.10.2"
  },
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "test": "jest test --coverage",
    "db:generate": "sequelize db:create",
    "db:migrate": "sequelize db:migrate",
    "test:db:setup": "npm run db:generate && npm run db:migrate"
  }
}
